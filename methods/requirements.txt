# inCCsight — unified Python requirements
# Install with: pip install -r requirements.txt
#
# Only DIRECT dependencies are listed here. All transitive packages
# (e.g. imageio, tifffile, pywavelets, networkx, lazy_loader, h5py, tqdm,
# torchmetrics, lightning-utilities, sympy, fsspec, jinja2, requests,
# urllib3, certifi, six, python-dateutil, pytz, packaging, attrs, …) are
# resolved automatically by pip from the packages below, so they no longer
# need to be pinned by hand.
#
# PyTorch must be installed separately (see README for CUDA variants):
#   CPU:       pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA 11.8: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121

# --- Core scientific stack ---
numpy>=1.26.4
scipy==1.11.4
pandas==2.1.4
scikit-image==0.22.0
scikit-learn==1.3.2
matplotlib>=3.8.0

# --- Neuroimaging ---
nibabel==5.2.1
dipy==1.9.0

# --- Computer vision (CC contour for thickness) ---
opencv-python-headless==4.9.0.80

# --- Deep learning (PyTorch installed separately) ---
monai==1.4.0
torchvision==0.19.1
pytorch-lightning==2.2.1

# --- Image I/O ---
Pillow==10.2.0

# --- Connectivity / labelling ---
connected-components-3d==3.12.4

# --- Model loading (.joblib shape-signature references) ---
joblib==1.3.2

# --- Optional (not installed by default; only used by inactive code paths) ---
# siamxt     # Max-Tree watershed markers — segmentation falls back to skimage when absent
# SimpleITK  # STAPLE mask fusion (segm_staple)
