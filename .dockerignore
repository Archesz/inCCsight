node_modules
build
.git
.gitignore
.env
.env.*
!.env.example

# Python environments
methods/venv
methods/roqs/venv
methods/CNNBased/venv
**/__pycache__
**/*.pyc
**/*.pyo

# Pipeline output (mounted as volumes)
data/
methods/csvs/*.csv
methods/csvs/groups.json

# NOTE: the ViT QC model (methods/models/*.pth) and the CNN checkpoint
# (methods/CNNBased/peso/*.ckpt) are intentionally NOT ignored here, so they
# are baked into the image by `COPY . .` when present in the build context.

# Subject data
subjects/

# Docs & misc
*.md
coverage/
.DS_Store
