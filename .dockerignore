node_modules
build
.git
.gitignore
.env
.env.*
!.env.example

# Python environments
methods/venv
methods/roqs/venv
methods/CNNBased/venv
**/__pycache__
**/*.pyc
**/*.pyo

# Pipeline output (mounted as volumes)
data/
methods/csvs/*.csv
methods/csvs/groups.json

# CNN checkpoint (mounted as volume / downloaded at runtime)
methods/CNNBased/peso/*.ckpt

# NOTE: the ViT QC model methods/models/*.pth is intentionally NOT ignored here
# so it gets baked into the image by `COPY . .` when present in the build context.

# Subject data
subjects/

# Docs & misc
*.md
coverage/
.DS_Store
